import React from "react";

const Cards = props => {
    console.log(props);

    return(
        <React.Fragment>
            This is a card ;)
        </React.Fragment>
    )
}

export default Cards;